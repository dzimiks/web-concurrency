package main;

public class Professor extends Tutor {

    public Professor(EventStream eventStream) {
        this.eventStream = eventStream;
    }

    @Override
    public String toString() {
        return super.toString();
    }
}
